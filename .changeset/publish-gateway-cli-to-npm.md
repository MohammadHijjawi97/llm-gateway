---
'manifest': minor
---

Publish the management CLI to npm as `@mnfst/gateway-cli`. Install it with `npm i -g @mnfst/gateway-cli` instead of building the monorepo; the command is still `mnfst`. Its version tracks the Manifest release it ships with.
