---
'manifest': patch
---

Point every repository URL at `mnfst/llm-gateway` after the rename. The GitHub stars endpoint, the self-hosted update check, the `docker/install.sh` download source and the Docker image source label no longer rely on GitHub's redirect from the old name.
